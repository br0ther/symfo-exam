var $ = require('jquery');

require('bootstrap-sass');

// import the function from greet.js (the .js extension is optional)
// ./ (or ../) means to look for a local file
// var greet = require('./greet');

// $(document).ready(function() {
//     $('h1').html(greet('Br0ther'));
// });
